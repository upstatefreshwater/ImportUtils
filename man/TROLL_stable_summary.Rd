% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TROLL_stable_summary.R
\name{TROLL_stable_summary}
\alias{TROLL_stable_summary}
\title{Summarize stable TROLL observations}
\usage{
TROLL_stable_summary(
  df,
  group_col = stationary_depth,
  summary_fn = median,
  digits = 4
)
}
\arguments{
\item{df}{A data frame containing sensor observations and
corresponding `_stable` flag columns.}

\item{group_col}{Column used to group observations (e.g.,
`stationary_depth`).}

\item{summary_fn}{Summary function applied to stable values.
Default is `median`.}

\item{digits}{Numeric. Number of decimal places to round result of `summary_fn`.}
}
\value{
A data frame with one row per group and summarized
  sensor values for each parameter.
}
\description{
Summarizes sensor values using only observations flagged as stable
(i.e., where corresponding `<sensor>_stable` columns are `TRUE`).
}
\details{
Intended to create final values from the \code{TROLL_profile_compiler}
function.

The function automatically detects data columns paired with
`_stable` flag columns and computes a summary statistic for
each parameter within the specified grouping variable.
}
